================================================================================
Single item
================================================================================

- boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)))

================================================================================
Two items
================================================================================

- boop
- boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content))
  (item
    (marker_task_pending)
    (content)))

================================================================================
item, child
================================================================================

- boop
  - boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (item
      (marker_task_pending)
      (content))))

================================================================================
item, multiple children
================================================================================

- boop
  - boop
  - boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (item
      (marker_task_pending)
      (content))
    (item
      (marker_task_pending)
      (content))))

================================================================================
item, body
================================================================================

- boop
words
here

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body)))

================================================================================
item body, child, sibling
================================================================================

- boop
words
here
  - boop
- boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body)
    (item
      (marker_task_pending)
      (content)))
  (item
    (marker_task_pending)
    (content)))

================================================================================
item, children with body
================================================================================

- A
  - A1
aa
  - A2

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (item
      (marker_task_pending)
      (content)
      (body))
    (item
      (marker_task_pending)
      (content))))

================================================================================
code block
================================================================================

- boop
```
let x = 0
x + 1
  - 2
```

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body
      (code_block
        (code_block_content)))))

================================================================================
code block language
================================================================================

- boop
```js
const x = 0
```

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body
      (code_block
        (code_block_language)
        (code_block_content)))))

================================================================================
empty code block
================================================================================

- boop
```
```

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body
      (code_block))))

================================================================================
item body, code
================================================================================

- boop
words
here
```
foobar()
  - not an item
```

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body
      (code_block
        (code_block_content)))))

================================================================================
item body, child, code, sibling
================================================================================

- boop
words
here
```
foobar()
  - not an item
```
  - boop
- boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body
      (code_block
        (code_block_content)))
    (item
      (marker_task_pending)
      (content)))
  (item
    (marker_task_pending)
    (content)))

================================================================================
item, indented body
================================================================================

- boop
  s boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body)))

================================================================================
items spaced
================================================================================

- boop

- boop

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content))
  (item
    (marker_task_pending)
    (content)))

================================================================================
item no content
================================================================================

- foo
-
words here

--------------------------------------------------------------------------------

(document
  (item
    (marker_task_pending)
    (content)
    (body)))
